name: Azure Login sample workflow

on: [push]

jobs:
  test-azure-login:
    runs-on: ubuntu-latest
    steps:
      - name: Login 
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_LOGIN }}

      - run: az account show